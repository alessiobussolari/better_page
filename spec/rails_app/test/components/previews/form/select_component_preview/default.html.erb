<div class="max-w-md mx-auto p-6 bg-white rounded-lg shadow">
  <h3 class="text-lg font-semibold text-gray-900 mb-4">Select Component</h3>

  <%= form_with model: Product.new, url: "#", method: :post do |f| %>
    <div class="space-y-4">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Category</label>
        <%= render BetterPage::Form::SelectComponent.new(
          form: f,
          field_name: :category,
          options_list: [["Electronics", "electronics"], ["Clothing", "clothing"], ["Home", "home"]],
          include_blank: true
        ) %>
      </div>
    </div>
  <% end %>
</div>
