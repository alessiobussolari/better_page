<%= render BetterPage::Ui::DrawerComponent.new(
  id: "drawer-no-header",
  title: nil,
  direction: :right,
  closable: false
) do |drawer| %>
  <% drawer.with_trigger do %>
    <button
      data-action="click->drawer#open"
      class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-gray-600 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500">
      Open Drawer without Header
    </button>
  <% end %>

  <div class="space-y-4">
    <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
      <h3 class="text-lg font-medium text-gray-900 mb-2">No Header Drawer</h3>
      <p class="text-sm text-gray-600">
        This drawer has no title and no close button (<code class="bg-gray-100 px-1 rounded">closable: false</code>).
      </p>
      <p class="text-sm text-gray-600 mt-2">
        You can still close it by:
      </p>
      <ul class="mt-2 text-sm text-gray-600 list-disc list-inside">
        <li>Clicking on the backdrop</li>
        <li>Pressing the ESC key</li>
        <li>Using the custom close button below</li>
      </ul>
    </div>

    <p class="text-gray-700">
      This is useful when you want full control over the drawer content and don't want the default header.
    </p>

    <button
      data-action="click->drawer#close"
      class="w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500">
      Close Drawer
    </button>
  </div>
<% end %>
