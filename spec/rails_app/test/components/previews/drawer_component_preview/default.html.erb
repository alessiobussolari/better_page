<button
  onclick="Stimulus.getControllerForElementAndIdentifier(document.querySelector('#drawer-default'), 'drawer').open()"
  class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
  Open Drawer (Right)
</button>

<%= render BetterPage::Ui::DrawerComponent.new(
  id: "drawer-default",
  title: "Drawer Title",
  direction: :right
) do %>
  <p class="text-gray-700">
    This is a drawer component that slides in from the right.
  </p>
  <p class="mt-4 text-gray-600">
    You can close it by:
  </p>
  <ul class="mt-2 list-disc list-inside text-gray-600">
    <li>Clicking the X button</li>
    <li>Clicking the backdrop</li>
    <li>Pressing the Escape key</li>
  </ul>
<% end %>
