{
  "$schema": "https://context7.com/schema/context7.json",
  "projectTitle": "BetterPage",
  "description": "A Ruby gem for building structured Page Objects in Rails with dry-schema validation and Turbo support.",
  "folders": ["context7", "docs", "guide"],
  "excludeFolders": ["spec", "tmp", "vendor"],
  "excludeFiles": [],
  "rules": [
    "No database queries in Pages - data passed via constructor",
    "No business logic - UI configuration only",
    "Use Hash-only structures, no OpenStruct/Struct",
    "Checkbox and radio fields must be in separate panels"
  ]
}
